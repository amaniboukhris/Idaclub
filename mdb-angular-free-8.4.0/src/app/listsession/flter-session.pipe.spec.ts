import { FlterSessionPipe } from './flter-session.pipe';

describe('FlterSessionPipe', () => {
  it('create an instance', () => {
    const pipe = new FlterSessionPipe();
    expect(pipe).toBeTruthy();
  });
});
